import styled from "styled-components";

export const PageHeadingBlock = styled.div`
  font-size: 24px;
  font-weight: bold;
  margin: 20px 0px;
  text-align: center;
  color: ${({ theme }) => theme.color.text};
`;
